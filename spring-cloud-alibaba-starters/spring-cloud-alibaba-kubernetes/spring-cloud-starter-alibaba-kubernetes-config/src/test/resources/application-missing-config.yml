spring:
  application:
    name: missing-config
  cloud:
    k8s:
      config:
        namespace: default
        configmaps:
          - name: missing-configmap
logging:
  level:
    com.alibaba.kubernetes.config: debug
