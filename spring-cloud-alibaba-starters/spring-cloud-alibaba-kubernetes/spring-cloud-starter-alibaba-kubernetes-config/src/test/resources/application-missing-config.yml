spring:
  application:
    name: missing-config
  cloud:
    alibaba-kubernetes:
      config:
        namespace: default
        configmaps:
          - name: missing-configmap
logging:
  level:
    com.alibaba.kubernetes.config: debug
