spring:
  application:
    name: client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      username: nacos
      password: nacos
    config:
      discovery:
        enabled: true
  profiles:
    active: dev