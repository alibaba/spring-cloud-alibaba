spring:
  application:
    name: client
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: 127.0.0.1:8848
    config:
      discovery:
        enabled: true
  profiles:
    active: dev