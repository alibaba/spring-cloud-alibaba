spring:
  application:
    name: integrated-provider
  cloud:
    nacos:
      config:
        file-extension: yaml
        server-addr: localhost:8848
        group: integrated-example
server:
  port: 8015
